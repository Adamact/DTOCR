# Runtime dependencies for the desktop app and OCR pipeline.
# Canonical source is pyproject.toml — `pip install -e .` is preferred.
# For the headless API and worker: pip install -e ".[web]"

numpy>=1.24
opencv-python>=4.8
openpyxl>=3.1
pandas>=2.0
pillow>=10.0
pypdfium2>=4.20
torch>=2.0
torchvision>=0.15
transformers>=4.35
sentencepiece>=0.1.99

# Optional, Windows GPU acceleration (DirectML) — see README.
# torch-directml
